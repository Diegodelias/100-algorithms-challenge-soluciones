//dado un string remplazar cada uno de sus caracteres por el que siegue en el alfabeto
//por ejemplo para elel string "crazy" la salida debería  ser dsbaz
//pistas split, indexOf(),  join
function cambiarPorLetraSiguiente(palabra) {
    var resultado;
    var arreglo = palabra.split('');
    resultado = palabra;
    //recorrer arreglo  y guardar  cada letra sumandole un uno a su posicion
    return resultado;
}
cambiarPorLetraSiguiente("loro");
